# GEMINI.md - プロジェクト全体 開発ガイドライン & 自律エージェント運用規則

本ワークスペース（`音響分析ソフト` スイート全体および配下の各アプリケーション群）における Antigravity / AI エージェントの自律動作ルール、Git 運用規約、品質検証方針を定義します。

---

## 1. ワークスペース構成と対象リポジトリ

本ワークスペースは複数の研究・教育用音響分析・言語学アプリケーションから構成されるマルチプロジェクト構成です。各ディレクトリは独立した Git リポジトリとして管理されています。

- **ルートリポジトリ**: `音響分析ソフト/` (`acoustic-annotator.git`)
  - `apps/annotator/` (Next.js フロントエンド + FastAPI バックエンド)
- **サブツール群 (独立リポジトリ)**:
  - `ipa-keyboard/` (`ipa-editor.git`): 国際音声字母入力エディタ (Vanilla HTML/JS, GitHub Pages)
  - `phonological-rule-editor/` (`phonological-rule-editor.git`): 音韻規則・音変化エディタ (KaTeX / SVG, GitHub Pages)
  - `syntax-tree-editor/` (`syntax-tree-editor.git`): 構文木エディタ (Canvas / SVG, GitHub Pages)
  - 今後 `apps/` やルート配下に追加される新規ツール

---

## 2. 自律実行（Agent Behavior: Always Allow）の運用原則

ユーザーがツールの自動承認（Always Allow）を有効化している前提で自律的に作業を推進します。  
無駄な確認対話を挟まず、**「自律的な実装 → 自動検証（ビルド・テスト・構文チェック） → 適切なリポジトリへのコミット＆プッシュ」** を完結させます。

### 🚨 絶対禁止事項（不可逆操作・データ破壊の防止）
1. **破壊的 Git 操作の禁止**:
   - `git push --force` (`-f`)
   - ユーザーからの明示的な指示がない限り `git reset --hard` や `git clean -fd`
   - リモートブランチやタグの削除
2. **無検証コミット・プッシュの禁止**:
   - 構文エラー（SyntaxError）、型エラー、ビルド失敗を放置したままコミット・プッシュしてはならない。
3. **スコープ外の改変禁止**:
   - 作業対象外のアプリやファイルに対して勝手なフォーマット変更や大規模リファクタリングを行わない。
4. **機密情報のコミット禁止**:
   - APIキー、認証トークン、環境設定（`.env`）の秘密情報をコミットしない。

---

## 3. Git 運用 & リポジトリ別コミットルール

各フォルダが独立した Git 管理下にあるため、**操作対象のディレクトリに合わせた適切なリポジトリでの Git 操作** を徹底します。

1. **カレントディレクトリの意識**:
   - `ipa-keyboard` 内の変更は `ipa-keyboard` 直下で commit/push する。
   - `phonological-rule-editor` 内の変更は同フォルダ直下で commit/push する。
   - `syntax-tree-editor` 内の変更は同フォルダ直下で commit/push する。
   - `apps/annotator` やルート直下の変更はワークスペースルート直下で commit/push する。
2. **コミットメッセージ規約**:
   - [Conventional Commits](https://www.conventionalcommits.org/) 形式を採用。
     - `feat:` 新機能追加
     - `fix:` 不具合・表示崩れの修正
     - `docs:` ドキュメント（README、Wiki等）の変更
     - `style:` コード動作に影響しない見た目・フォーマット修正
     - `refactor:` リファクタリング
     - `test:` テストスクリプトの追加・更新
3. **アトミックコミット（最小単位コミット）**:
   - 万が一不具合が生じた際に `git revert` や `git reset` で最小限の巻き戻しができるよう、1機能・1修正ごとにコミットを分ける。
4. **プッシュ方針**:
   - 作業が完了し検証をパスした段階で、該当リポジトリの `origin/main`（またはアクティブな作業ブランチ）へ push してリモートと同期する。

---

## 4. 品質保証・自動検証プロトコル

変更を加えた際は、完了報告およびコミットを行う前に必ず検証を実施すること。

- **Web ツール群（Vanilla JS / 静的アプリ）**:
  - `node -c <js_files>` 等で構文エラーを検証。
  - レスポンシブ対応（モバイル幅 375px〜420px での表示崩れやスクロール不正がないか CSS の確認）。
  - 各アプリ固有のコアデータ（IPA 記号、音韻規則パーサー、構文木レンダラー）の破損がないこと。
- **フルスタックアプリ（Next.js / FastAPI）**:
  - Frontend: `npm run build` または型チェック・リントを実行。
  - Backend: `uv run pytest` または Python スクリプトのインポート・構文テストを実施。

---

## 5. エージェントの行動規範

1. **自律的完結**: 必要なファイル作成・バグ修正・依存解決は許可を求めず自ら実行し、結果を報告する。
2. **透明性のある報告**: 完了時は「何を変更したか」「どのように検証したか」「どのリポジトリにどのコミットハッシュでプッシュしたか」を簡潔・明瞭に提示する。
3. **ロールバック耐性の確保**: いつでも安全に以前の状態へ戻せるクリーンな Git 履歴を維持する。
